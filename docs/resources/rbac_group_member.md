---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "anthropic_rbac_group_member Resource - Anthropic Enterprise"
subcategory: "Claude Enterprise"
description: |-
  Adds a user to an RBAC group in a Claude Enterprise organization. Requires enterprise_api_key.
  Membership of SCIM-managed groups cannot be changed through the API. Import with group_id/user_id.
---

# anthropic_rbac_group_member (Resource)

Adds a user to an RBAC group in a Claude Enterprise organization. Requires `enterprise_api_key`.

Membership of SCIM-managed groups cannot be changed through the API. Import with `group_id/user_id`.

## Example Usage

```terraform
resource "anthropic_rbac_group" "engineering" {
  name = "Engineering"
}

data "anthropic_user" "alice" {
  email = "alice@example.com"
}

resource "anthropic_rbac_group_member" "alice" {
  group_id = anthropic_rbac_group.engineering.id
  user_id  = data.anthropic_user.alice.id
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `group_id` (String) RBAC group id (`rbac_group_...`).
- `user_id` (String) User id (`user_...`).

### Read-Only

- `created_at` (String) Timestamp the membership was created (RFC 3339).
- `email` (String) Email of the member.
- `id` (String) Composite id `group_id/user_id`.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# group_id/user_id
terraform import anthropic_rbac_group_member.alice rbac_group_01ExampleGroupId00000000/user_01ExampleUserId0000000000
```
