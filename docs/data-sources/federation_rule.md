---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "anthropic_federation_rule Data Source - Anthropic Enterprise"
subcategory: "Service Accounts and Federation"
description: |-
  Looks up one workload identity federation rule by id or name. Requires oauth_token.
---

# anthropic_federation_rule (Data Source)

Looks up one workload identity federation rule by id or name. Requires `oauth_token`.

## Example Usage

```terraform
data "anthropic_federation_rule" "deploy" {
  name = "gha-deploy"
}

output "deploy_rule_workspaces" {
  value = data.anthropic_federation_rule.deploy.workspace_ids
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Optional

- `id` (String) Rule id (`fdrl_...`). Exactly one of `id` or `name` must be set.
- `name` (String) Slug name; only live (unarchived) rules are matched.

### Read-Only

- `applies_to_all_workspaces` (Boolean) Whether the rule is enabled for every workspace.
- `archived_at` (String) Archive timestamp; null while live.
- `audience` (String) Exact `aud` match.
- `claims` (Map of String) Exact-match claims.
- `condition` (String) CEL condition over claims.
- `created_at` (String) Creation timestamp.
- `description` (String) Description.
- `issuer_id` (String) Issuer id.
- `issuer_name` (String) Issuer name.
- `oauth_scope` (String) Scope of minted tokens.
- `service_account_id` (String) Target service account id.
- `service_account_name` (String) Target service account name.
- `subject_prefix` (String) Matched `sub` prefix (exact unless it ends in `*`).
- `token_lifetime_seconds` (Number) Lifetime of minted tokens.
- `workspace_ids` (List of String) Workspaces the rule is enabled for.
