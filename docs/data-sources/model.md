---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "anthropic_model Data Source - Anthropic Enterprise"
subcategory: "Models"
description: |-
  Reads one model.
  ~> This data source needs api_key (a regular workspace or organization API key). An admin_api_key or oauth_token is rejected: the endpoint requires an inference scope.
---

# anthropic_model (Data Source)

Reads one model.

~> This data source needs `api_key` (a regular workspace or organization API key). An `admin_api_key` or `oauth_token` is rejected: the endpoint requires an inference scope.

## Example Usage

```terraform
data "anthropic_model" "opus" {
  id = "claude-opus-5"
}

output "context_window" {
  value = data.anthropic_model.opus.max_input_tokens
}

# The flat capabilities map carries each capability's own support flag.
output "supports_thinking" {
  value = data.anthropic_model.opus.capabilities["thinking"]
}

# Some capabilities nest sub-capabilities that a map of booleans cannot
# express, so the payload is also exposed verbatim.
output "supports_compaction" {
  value = jsondecode(data.anthropic_model.opus.capabilities_json).context_management.compact_20260112.supported
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `id` (String) Model id, for example `claude-opus-5`. Reading a model that does not exist fails the plan, which is the point: it catches a retired or mistyped id before an apply reaches it.

### Read-Only

- `capabilities` (Map of Boolean) Top-level support flag per capability, for example `code_execution` or `thinking`. Use this for `lookup(...)` checks in configuration.
- `capabilities_json` (String) The capabilities payload verbatim, as JSON. Some capabilities nest sub-capabilities that the flat `capabilities` map cannot express, such as `context_management.compact_20260112`; reach them with `jsondecode`.
- `created_at` (String) Release timestamp (RFC 3339).
- `display_name` (String) Human-readable name.
- `max_input_tokens` (Number) Largest context window the model accepts.
- `max_tokens` (Number) Largest output the model will produce.
